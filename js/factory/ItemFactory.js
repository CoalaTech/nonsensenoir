// Move functions from ObjectManager to this class
// Move item interaction to Item class
// Bind everything through signals =]

export default class ItemFactory {

	constructor(config) {

		this.config = config;

	}

	static createObject() {
		
	}

}